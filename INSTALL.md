On debian-like systems like the Raspian (Raspberry Pi), use

# Upgrade your system before installing the requisites

sudo apt-get update
sudo apt-get upgrade

# Install requisites

sudo apt-get install php5 mplayer at
